weights:
  required_keywords: 0.50   # all "required" keywords must appear
  preferred_keywords: 0.30  # coverage ratio of "preferred" keywords
  must_not_violations: 0.20 # penalty per "must_not_contain" hit
pass_threshold: 0.65
